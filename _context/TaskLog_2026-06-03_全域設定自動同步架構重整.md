# 工作紀錄 2026-06-03 — 全域設定自動同步架構重整

## 1. 本次完成項目

1. **AI 效能衰退觀察與因應** — 記錄 claude-sonnet-4-6 本次 session 的開場協議未執行、同對話歸因錯誤問題，新增全域 lesson「AI 效能與可信度衰退因應」至 `wtf-config/GLOBAL.md` 與 `wtf-config/AGENTS.md`。
2. **精簡用語原則** — 新增明確定義（禁止確認語、重述、完成後總結、過渡語）至 GLOBAL.md 與 AGENTS.md。
3. **~/.claude symlink 修復** — 修復兩個斷裂 symlink（CLAUDE.md 與 skills），後改為實體複製。
4. **sync_config.py 擴充**：
   - 新增 `deploy_claude_dir()`：sync 同時複製 `CLAUDE_CODE.md` → `~/.claude/CLAUDE.md`、`skills/` → `~/.claude/skills/`。
   - 新增 `check_claude_dir()`：check 涵蓋 `~/.claude/` 狀態。
   - 修 Windows cp950 編碼（`sys.stdout.reconfigure`）。
5. **extra-scan-dirs.txt** — 建立 `wtf-config/extra-scan-dirs.txt`（`hostname:path` 格式），納管 `/Users/coma/Git_work/` 的 4 個 repo（Assembly_Plant_Mobile_Guide、Planner2Line、Remotion_fun、claude_CDIC_O4）。
6. **wtf-sync.sh hook** — 建立 `~/.claude/wtf-sync.sh`，每次對話開始自動清 lock 檔、git pull、sync，並回報結果。加入 `settings.json` UserPromptSubmit hook。已驗證正常運作。
7. **settings.json 清理** — 移除 `additionalDirectories` 中的舊 `git_folder` 路徑。
8. **舊 symlink 架構清理** — commit 刪除 `._agents/skills/` 與 `.claude/skills` symlink，改為全域實體複製。

## 2. 未解決問題（依優先度）

- **P1 — Drive projects FOREIGN（7 個）**：舊版 AGENTS.md 無 WTF-AUTOGEN 標頭，sync 不覆蓋。需決定是否納入自動管理（加 --force 或手動清空）。
- **P1 — Windows ~/.claude/ 設定未確認**：不確定 Windows Claude Code 是否有讀到 WTF 設定。需到 Windows 執行 `Get-Content $env:USERPROFILE\.claude\CLAUDE.md` 確認。
- **P1 — Windows wtf-extra-dirs.txt**：若 Windows 有本機 repo，需在 `wtf-config/extra-scan-dirs.txt` 補 `DESKTOP-7SF21LR:E:\路徑`。
- **P2 — AGENTS.md typechange**：git 將 WTF root 的 AGENTS.md 記為 symlink，實際為實體檔，持續顯示 typechange。需確認是否需整理。
- **P2 — 7 個 Drive projects FOREIGN**：T5 清孤兒 `AGENTS (1).md`、各專案 _context/INDEX.md、T2 skills 漂移等前次交接待辦未動。

## 3. 主要輸入檔案

- `wtf-config/GLOBAL.md`、`wtf-config/AGENTS.md`（已更新）
- `wtf-config/sync_config.py`（已擴充）
- `wtf-config/extra-scan-dirs.txt`（新建）
- `~/.claude/wtf-sync.sh`（新建）
- `~/.claude/settings.json`（已更新）

## 4. 下一步建議

1. 到 Windows 確認 `~/.claude/CLAUDE.md` 狀態。
2. 在 `wtf-config/extra-scan-dirs.txt` 補 Windows 本機 repo 路徑。
3. 決定 7 個 FOREIGN 專案的 AGENTS.md 處理方式。
4. 承前次交接：T5 清孤兒、T2 skills 漂移、T6/T7 收尾。
