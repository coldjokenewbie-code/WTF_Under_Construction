# 工作紀錄 2026-06-07 — Codex/Antigravity 開場載入全域設定與 skills 對等

> 目標：讓 Codex、Antigravity(agy) 跟 Claude 一樣在 session 開場自動載入 WTF 全域設定、並用原生 skills。
> 方法：Claude 任 Tech Lead，用 headless CLI（codex exec／agy --print）與兩工具三輪討論定案。
> 作者：[Claude@Mac]

## 1. 診斷（已實證）

- Codex/Antigravity **開場根本沒載入 WTF 全域設定**：其全域指令檔是 5/24 建、指向已移出舊 Drive 路徑的 **dangling symlink**（`~/.codex/CODEX.md`、`~/.gemini/GEMINI.md`、`~/.gemini/AGENTS.md`），且 `~/.codex/AGENTS.md` 是 0-byte 空檔。
- 根因：`deploy_other_tools()` 只部署 skills＋wtf-root.txt，**從不管全域指令檔**，斷鏈一個月無人察覺。

## 2. 各工具原生機制（兩工具自證）

| 工具 | 開場原生讀的全域指令檔 | 原生 skills |
|---|---|---|
| Claude | `~/.claude/CLAUDE.md` | ✓ lazy-load |
| Codex | `~/.codex/AGENTS.md`（**不讀 CODEX.md**，`codex debug prompt-input` 實證） | ✓ 自動列、觸發才讀 |
| Antigravity(agy) | `~/.gemini/GEMINI.md` | ✓ 同上 |

- SSOT 的 `CODEX.md`／`GEMINI.md` 早已是 bootstrap 檔（讀 wtf-root→GLOBAL+AGENTS+_context），只是部署到錯檔名＋斷鏈。

## 3. 已完成（本機 Mac）

- **`sync_config.py`**：`deploy_other_tools()` 擴充——實體複製 `CODEX.md→~/.codex/AGENTS.md`、`GEMINI.md→~/.gemini/GEMINI.md`，複製前拆同名斷鏈、清 stale（`~/.codex/CODEX.md`、`~/.gemini/AGENTS.md`）；`cmd_check` 增驗兩檔存在非空。
- **`wtf-config/CODEX.md`、`GEMINI.md`**：載入方式改「實體複製」、刪「開場強制讀全部 SKILL.md」改原生 lazy-load。
- **`ai-team` skill**：新增「同機優先用 headless CLI 直驅」段（`claude -p`／`codex exec`／`agy --print`），信號檔降為跨機/GUI/持久化 fallback。
- Mac sync 部署 + check OK；**端對端驗證**：從中性目錄 headless 問 codex/agy，兩者皆正確讀到全域 bootstrap（codex 知禁「您」、agy 確認 lazy-load）。

## 4. 派 claude@windows（待辦）

> claude@windows 下次開場讀到即執行。完成後回寫打勾。**這是 per-machine 部署，Mac 做完 Windows 仍要各做一次。**

- [ ] **Windows 同步部署**：`python wtf-config/sync_config.py sync`（會寫 `~/.codex/AGENTS.md`、`~/.gemini/GEMINI.md`、清 Windows 端對應斷鏈 symlink）。
- [ ] **驗證**：`python wtf-config/sync_config.py check`，確認「其他工具全域指令」段兩檔 OK。
- [ ] **功能測**（可選）：Windows 上 `codex exec`／`agy --print` 從非 WTF 目錄問「你開場載入哪些 SSOT」，確認讀到 GLOBAL+AGENTS。
- [ ] 若 Windows 端 codex/gemini 未安裝（home 不存在）→ sync 自動跳過，無需處理。

## 4b. 後續（用戶找時間測）

- [ ] **Claude Chat 雲端設定**：比照 Cowork，Chat 也讀不到本機 skills。待測 Chat 能否 fetch `CLAUDE_CHAT.md` raw URL；可行則把「skill 清單＋raw URL」機制也加進 `CLAUDE_CHAT.md`，貼 URL 到 Chat project instruction。（先放待辦，用戶測完再做）

## 4c. 已定案：專案 skill 統一目錄

- **專案內 skill 一律放 `._agents/skills/`**（工具中立）。原生自動清單只含全域 `~/.<tool>/skills/`，**不含 `._agents/skills/`** → 進專案時各工具主動 lazy-list 該目錄（列名稱＋描述，觸發才讀）。已廢除舊 symlink 機制。
- 已更新：AGENTS.md、CODEX.md、GEMINI.md、CLAUDE_CODE.md 的 skill 載入段一致指向此規則。

## 5. 通用原則（已存記憶 cross-system-sync-always）

WTF 任何設定/部署更新永遠考慮 Win+Mac 雙系統；很多本機部署是 per-machine，**每台機器各做一次**，收尾必寫 Windows 待辦。
