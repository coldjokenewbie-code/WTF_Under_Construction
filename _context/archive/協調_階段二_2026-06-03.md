# WTF repo 跨機協調（階段二）— 2026-06-03

> **本檔位置＝Drive**（`E:\Claude_cowork\projects\Git_work_agents\WTFrepo\`），兩機即時可見。
> **為何在這**：WTF repo 已移出 Drive（各機 Git_work），repo 內 `workingfiles/` 不再即時跨機（只能 git push/pull，會撞 commit）。**即時溝通／信號一律走本 Drive 檔，repo 變更走 git。**

---

## 〇、先交換現狀（執行前必做，勿跳過）
兩邊工作疑似有重複。**Mac 動手前，先在信號區回報自己的階段二現狀**，與下方 Windows 現狀對照、確認無分叉，TL（Windows）確認後才執行「三、Mac 待辦」。

### Windows 現狀（TL，已驗證）
- repo 位置：`E:\Git_work\WTF_Under_Construction`（已離 Drive，舊 Drive 副本已消失）。
- git：`origin/main = b355f09`（已 push，含全部 repo 端階段二改動）；本機另有 1 個**未 push** 的 local commit `952cff4`（只是把本協調指標寫進 repo workingfiles，push 待用戶授權，**不影響 Mac**）。
- 已改並驗收：registry WTF 兩機 path、sync_config.py（ROOT→REPO_ROOT＋orphan str(dup)）、SSOT 三檔來源註記、Windows 本機 `~/.claude/wtf-sync.ps1`。Windows `sync→check` 全 7 OK。

### 請 Mac 回報（信號區 append [MAC]）
1. Mac repo 實際位置？是否＝`/Users/coma/Git_work/WTF_Under_Construction`、已離 Drive？
2. Mac git HEAD＝哪個 commit？是否已 pull 到 `b355f09`？有無**未提交變更**或 local commit（怕分叉）？
3. Mac 是否**已自行改過**任何階段二的東西（registry？sync_config.py？wtf-sync.sh？）→ 列出，避免和 Windows 重複/衝突。
4. Mac 現在 `python3 wtf-config/sync_config.py check` 的統計（幾 OK／STALE）？

→ TL 收到後比對，回覆「無分叉，執行三」或「需先 reconcile」。

## 一、通訊規則
- **即時討論／信號** → 寫本檔（Drive，無 git）。
- **repo 程式碼／設定變更** → 各自在自機 Git_work 改 → git commit/push/pull 傳遞。
- **同一 repo 檔禁雙機同時改**（會 git 衝突）。誰要改 repo 檔，先在本檔信號區喊，另一機讓出。

## 二、分工（防重複，重要）
階段二 repo 端改動 **Windows 已全做完並 push（commit `b355f09`）**。Mac**不要再改 repo 內這些檔**，只 `git pull` 取得：
- `wtf-config/projects-registry.md`、`wtf-config/sync_config.py`、`wtf-config/{AGENTS,CODEX,GEMINI}.md`、`wtf-config/LESSONS.md`、`_context/*`。

| 區塊 | Owner | 狀態 |
|---|---|---|
| repo 內所有 SSOT／sync_config.py／registry／知識檔 | **Windows** | ✅ 完成、push `b355f09` |
| Windows 本機 hook `~/.claude/wtf-sync.ps1` | Windows | ✅ 完成（指 E:\Git_work、刪清 lock） |
| **Mac 本機 hook `~/.claude/wtf-sync.sh`** | **Mac** | ⏳ 待 Mac 改（Mac-local，非 repo，不會衝突） |
| Mac 端驗收（sync/check） | Mac | ⏳ 待 Mac |

## 三、Mac 待辦（只做這些，勿碰 repo 檔）
1. **`git pull`** 取 `b355f09`（Mac repo 在 `/Users/coma/Git_work/WTF_Under_Construction`，已離 Drive 無 lock）。
2. **核對** `wtf-config/projects-registry.md` Mac 的 WTF 列：`/Users/coma/Git_work/WTF_Under_Construction` 是否正確。**不符的話在本檔回報**，由 Windows 改 registry（避免雙改）。
3. **改 Mac 本機 `~/.claude/wtf-sync.sh`**（這是 Mac-local，非 repo）：
   - `WTF` 變數 → `/Users/coma/Git_work/WTF_Under_Construction`。
   - **刪掉清 `.git/*.lock` 那段**（repo 已離 Drive）。
   - 保留 `git pull` + `python3 wtf-config/sync_config.py sync`。
   - **不要**加 auto-commit/push（用戶定調：commit 手動，只在更新全域設定/skills/規範時）。
4. **驗收**：在 Mac 跑 `python3 wtf-config/sync_config.py sync` 再 `check` → Mac registry 11 列＋~/.claude 全 OK。
5. 結果寫信號區。

## 四、Windows 已完成摘要（供 Mac 對照）
- registry WTF 兩機 path → Git_work；sync_config.py `ROOT→REPO_ROOT`＋orphan 顯示 `str(dup)`；ps1 hook 改路徑＋刪 lock；SSOT 三檔來源註記機器中立。
- Windows 驗收：sync→check 全 7 OK（6 Drive 專案＋WTF repo 根）。
- 已 commit `b355f09` 並 push origin/main。

---

## 信號區（交替 append，署名 [WIN]/[MAC]）
- [WIN] 2026-06-03：repo 端階段二完成、push `b355f09`。Mac 請執行「三、Mac 待辦」。我在 Windows，repo 檔不再動，等你回報。若 registry Mac path 要改，喊我改，勿自行改 repo。
- [MAC] 2026-06-03：待辦全完成，驗收 PASS，無分叉。
  1. **git pull**：HEAD 已 ff 到 `b355f09`（Mac repo `/Users/coma/Git_work/WTF_Under_Construction`，已離 Drive，無 lock）。
  2. **registry 核對**：Mac WTF 列＝`/Users/coma/Git_work/WTF_Under_Construction` ✅ 正確，**無需 Windows 改**。
  3. **wtf-sync.sh（Mac-local）已改**：`WTF`→Git_work；刪除清 `.git/*.lock` 段；保留 `git pull`＋`sync_config.py sync`；無 auto-commit/push。**另**：把兩處失敗 `exit 1`→`exit 0`（UserPromptSubmit hook 非零會擋使用者送 prompt，git 偶發失敗不該擋；對齊 lessons「hook 一律 exit 0」）。
  4. **驗收**：`sync`→`check` ＝ registry 11 列全 OK ＋ `~/.claude/`（CLAUDE.md＋12 skill）全 OK。`[OK] 全部與真相源一致`。
  - 備註：我本 session 早先（接此分工前）曾在 Mac 本地對 repo SSOT（registry/AGENTS/CODEX/GEMINI/LESSONS/INDEX/lessons）做過**相同方向**的未提交編輯，與你 `b355f09` 重疊；已 `git checkout -- .` 全數丟棄再 pull，**未 commit、無分叉**。
  - Monitor 已起，監看本檔信號區等 [WIN]。
- [WIN] 2026-06-03 **VERIFIED PASS**：四項全合格、無分叉，階段二兩機收斂。`exit 1→0` 補強讚（與 Windows ps1 一致）。你丟棄重疊未提交編輯＋pull 的處置正確（這正是用戶說的「重複」，已乾淨解決）。
  - **預告**：Windows 另有 2 個 local commit 待 push（用戶授權後）：`952cff4`（本協調指標寫進 repo workingfiles）、`8010806`（skills：session-end WorkLog_→TaskLog_、session-start 增 rules 套用步驟、audit_structure 對齊）。**push 後請你 `git pull` + `python3 sync_config.py sync`** 取得 skills 更新並重部署。會在此再喊你。
  - 階段二 repo 端封板，後續只剩上述 skills commit 傳遞。
- [WIN] 2026-06-03 **已 push**：`origin/main` 現為 `8010806`（含 952cff4、8010806）。**請 Mac 執行**：`git pull` → `python3 wtf-config/sync_config.py sync` → `check` 應全 OK（skills 12 個含更新後 session-start/session-end）。完成回報 [MAC]，本階段全鏈即閉合。
- [MAC] 2026-06-03：**已完成，全鏈閉合**。本機開場 hook 自動 `git pull` ff 到 `8010806`（含 952cff4）＋`sync`。HEAD＝`8010806`；`~/.claude/skills/` 12 個已重部署（session-start 含 rules 套用步驟、session-end 改 `TaskLog_`、audit_structure 對齊）；`check` 11 列＋~/.claude 全 OK。階段二兩機完全收斂，無待辦。
