# 交接文件 2026-06-02 — 設定載入與交接狀態核對

## 1. 現況摘要
- SSOT 設定已載入（GLOBAL.md + AGENTS.md）；設定檢查 7/7 AGENTS.md 一致。
- 前一份交接（T1/T6/T7/結案規範）待辦 1「規則 commit」已完成，working tree 乾淨（HEAD `5b98a90`）。
- 本 session 未動任何實作待辦，僅完成狀態核對與本交接產出。

## 2. 檔案位置
- 工作紀錄：`_context/TaskLog_2026-06-02_設定載入與交接狀態核對.md`
- 前一份交接：`_context/Handover_2026-06-02_設定整合T1T6T7與結案規範.md`（已被接手，依規則應移 `_context/archive/`，待確認）
- SSOT：`wtf-config/`（GLOBAL.md、AGENTS.md、LESSONS.md、machines.md、sync_config.py、audit_structure.py、organize_files.py、skills/）

## 3. 緊急修復
- 無 P0。

## 4. 技術細節
- `sync_config.py check` 在 Windows（cp950）會因 `✓` 字元 UnicodeEncodeError 中斷；執行前須 `$env:PYTHONIOENCODING="utf-8"`（PowerShell）。建議改腳本內固定 stdout 編碼。
- 真相源：`wtf-config/AGENTS.md`；掃描範圍：`projects/`。
- `sync_config.py` 範圍僅限 `projects/*/AGENTS.md`，不處理 `~/.claude/CLAUDE.md` 與 `~/.claude/skills`。
  - **Mac 現況**：兩者仍為 symlink（`wtf-config/CLAUDE_CODE.md` 與 `wtf-config/skills`）。
  - **Windows 現況**：不確定 `~/.claude/` 是否讀到 WTF 設定，或靠 project-level `.claude/CLAUDE.md`。
  - **待確認（Windows）**：開啟 Claude Code，確認 `~/.claude/CLAUDE.md` 是否存在、內容是否為 WTF 設定。若無，需決定：擴充 sync_config.py 覆蓋 `~/.claude/`，或手動複製一次。
  - **待執行（Windows）**：若有本機 repo（不在 Drive 內），建立 `%USERPROFILE%\.claude\wtf-extra-dirs.txt`，每行一個目錄路徑，sync 即自動納入。

## 5. 後續待辦（承前份交接，未變）
1. **T5**：清孤兒 `AGENTS (1).md`（刪前確認）。
2. 各專案建 `_context/INDEX.md`（進場先讀）。
3. **T2 skills 漂移**：盤點所有 live skill 與 SSOT 對齊，含 handover skill 改 `WorkLog_`→`TaskLog_`。
4. **T6/T7 收尾**：26 項 MANUAL 搬檔（`output/`→`outputs/` 改名須先 grep 引用）、lessons 逐專案彙整進 `wtf-config/LESSONS.md`。
5. 承前次 repo 移出 Drive：lesson「Drive 同步 .git 會損壞 repo」寫進 GLOBAL.md、修 lesson-add skill 路徑、notebooklm-skill remote 決策、跨機部署。
6. **P2**：修 `sync_config.py` 編碼。
