# 工作紀錄 2026-06-02 — 設定載入與交接狀態核對

## 1. 本次完成項目
1. 載入 SSOT 設定：`wtf-config/GLOBAL.md` + `wtf-config/AGENTS.md`。
2. 設定檢查：`python wtf-config/sync_config.py check` → 7 個 AGENTS.md 全部與真相源一致（OK: 7）。
   - 註：腳本在 Windows cp950 環境會 UnicodeEncodeError，須先 `$env:PYTHONIOENCODING="utf-8"` 再跑。
3. 核對前一份交接（`Handover_2026-06-02_設定整合T1T6T7與結案規範.md`）待辦，實查 git：
   - 待辦 1「規則 commit」**已完成** — 交接後另有 commit `98fb701`、`5b98a90`，working tree 乾淨。
4. 確認 handover skill 仍用 `WorkLog_`，與 SSOT 裁定的 `TaskLog_` 衝突（= T2 skills 漂移未整治）；本次依使用者裁示改用 `TaskLog_`。

## 2. 未解決問題（依優先度）
- **P1 — T2 skills 漂移**：handover skill 命名前綴（WorkLog_ vs SSOT 的 TaskLog_）未對齊。tasklog-naming 已入 SSOT，其餘 skill 待逐一盤點對齊。
- **P2 — sync_config.py 編碼**：Windows 下需手動設 UTF-8 才能跑 check，建議在腳本內固定 `sys.stdout.reconfigure(encoding='utf-8')`。

## 3. 主要輸入檔案
- `projects/WTF_Under_Construction/wtf-config/GLOBAL.md`、`AGENTS.md`
- `projects/WTF_Under_Construction/wtf-config/sync_config.py`
- `projects/WTF_Under_Construction/_context/Handover_2026-06-02_設定整合T1T6T7與結案規範.md`

## 4. 下一步建議
依風險低→收斂快排序：
1. T5 清孤兒 `AGENTS (1).md`（刪前確認）。
2. 各專案建 `_context/INDEX.md`。
3. T2 skills 漂移盤點對齊（含 handover skill 改 TaskLog_）。
4. T6/T7 收尾：26 項 MANUAL 搬檔、lessons 逐專案彙整進 `wtf-config/LESSONS.md`。
5. 修 sync_config.py 編碼（P2）。
