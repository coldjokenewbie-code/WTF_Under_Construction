# 交接文件 2026-06-03 — 全域設定自動同步架構重整

## 1. 現況摘要

- 全域設定自動同步已建立：每次 Claude Code session 開始，`~/.claude/wtf-sync.sh` 自動執行 git pull + sync，並在對話中回報結果。
- `sync_config.py` 現在覆蓋 `projects/*/AGENTS.md`、Git_work repos、以及 `~/.claude/CLAUDE.md` 與 `~/.claude/skills/`。
- Mac 上 `~/.claude/` 已為實體檔案（非 symlink）。
- Windows 端的設定狀態尚未確認。

## 2. 檔案位置

- 工作紀錄：`_context/TaskLog_2026-06-03_全域設定自動同步架構重整.md`
- 前次交接：`_context/Handover_2026-06-02_設定載入與交接狀態核對.md`
- Hook 腳本：`~/.claude/wtf-sync.sh`（本機，不在 repo）
- 額外掃描目錄：`wtf-config/extra-scan-dirs.txt`（在 repo，hostname:path 格式）
- SSOT：`wtf-config/`（GLOBAL.md、AGENTS.md、CLAUDE_CODE.md、skills/、sync_config.py）

## 3. 緊急修復

- 無 P0。

## 4. 技術細節

**wtf-sync.sh 架構**
- 位置：`~/.claude/wtf-sync.sh`（本機，需各機器各自建立）
- 觸發：`settings.json` UserPromptSubmit hook
- 冷卻：5 分鐘（`/tmp/wtf_sync_last` 時間戳）
- 流程：清 `.git/*.lock` → `git pull` → `python3 wtf-config/sync_config.py sync`
- Drive 在 `.git/refs/remotes/` 會產生 `.lock` 殘留，腳本已自動清理

**sync_config.py 掃描範圍**
- `Claude_cowork/projects/*/AGENTS.md`
- `wtf-config/extra-scan-dirs.txt` 中本機 hostname 對應的目錄
- `~/.claude/CLAUDE.md`（來源：`wtf-config/CLAUDE_CODE.md`）
- `~/.claude/skills/`（來源：`wtf-config/skills/`）

**FOREIGN 判斷**：無 `WTF-AUTOGEN` 標頭的 AGENTS.md 不會被覆蓋，需手動處理。

**Windows 部署（待執行）**
1. 確認 `%USERPROFILE%\.claude\CLAUDE.md` 是否存在且為 WTF 設定
2. 建立 `%USERPROFILE%\.claude\wtf-sync.sh`（或 `.ps1`）等效腳本
3. 在 `wtf-config/extra-scan-dirs.txt` 補 `DESKTOP-7SF21LR:E:\<路徑>`
4. Windows Claude Code `settings.json` 加入對應 hook

## 5. 後續待辦

1. **Windows 確認**：`Get-Content $env:USERPROFILE\.claude\CLAUDE.md` 確認設定存在。
2. **Windows extra-scan-dirs**：補本機 repo 路徑到 `wtf-config/extra-scan-dirs.txt`。
3. **Windows hook**：建立等效的 wtf-sync 腳本並加入 settings.json。
4. **FOREIGN 決策**：7 個舊版 AGENTS.md 是否納入自動管理。
5. 承前次：T5 孤兒清理、T2 skills 漂移、T6/T7 收尾、各專案 _context/INDEX.md。
