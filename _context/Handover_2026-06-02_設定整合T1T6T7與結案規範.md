# 交接文件 2026-06-02 — T1 register + T6/T7 規則落地 + 結案命名規範

## 1. 本 session 已完成
1. **T1**：本機 `register` 寫入 `wtf-config/machines.md`（DESKTOP-7SF21LR / Windows 10 / `E:\Claude_cowork`，別名待填）。
2. **T6/T7 規則寫入 SSOT（`wtf-config/GLOBAL.md`）**：
   - 輸出夾統一複數 `outputs/`；舊版進 `outputs/OLD/`，禁多版本平鋪。
   - 根目錄只放設定/入口檔，其餘歸子夾。
   - 新增 `_context/INDEX.md`（進場先讀）、`_context/archive/`、Handover 統一（廢 Handoff）。
   - lessons 兩層（工作層 `_context/lessons-learned.md` + 雲端 `wtf-config/LESSONS.md`）。
3. **雲端 lessons 索引**：新增 `wtf-config/LESSONS.md`（指標式，彙整各專案教訓）。
4. **兩支腳本**（`wtf-config/`）：
   - `audit_structure.py`：結構/檔名稽核，report-only。
   - `organize_files.py`：搬檔 dry-run / `--apply`，AUTO/MANUAL 分級。
5. **搬檔 --apply**：11 項 AUTO 全搬（南科舊版 pptx/xlsx→OLD、截圖→_screenshots、腳本→tools），0 衝突。
6. **結案命名規範裁決**（使用者拍板）：工作紀錄統一 `TaskLog_`；結案 = 改前綴 `ClosedTaskLog_` **且**移 `_context/archive/`（兩者都要）。Handover 接手後移 `_context/archive/`（原 skill 為 workingfiles/old，已改）。同步修正 `GLOBAL.md`、`tasklog-naming` skill（live + 新建 `wtf-config/skills/tasklog-naming/` SSOT 副本）。
7. **commit**：`5fe0c7b` 已 push origin/main（T6/T7 首批）。本 session 後段（結案規範）尚未 commit。

## 2. 後續待辦（依優先序，承接前兩份交接）
1. **規則 commit**：GLOBAL.md(結案段) + tasklog-naming skill(2 處) + 本交接歸檔尚未 commit。
2. **T6/T7 收尾**：
   - 26 項 MANUAL 搬檔（成果/素材難分、各組最新版、`output/`→`outputs/` 目錄改名須先 grep 程式碼引用）。WTF repo **外**的專案搬移屬各自 repo/Drive。
   - **各專案建 `_context/INDEX.md`（INDEX 機制，方案已定）**：
     - 用途：每專案現況總覽入口，進場先讀它、不必掃全 `_context/`；結案/交接/session-end 時更新。
     - 模板 4 區塊：① 當前狀態（一句話）② 進行中任務（checklist）③ 最新交接（→ `_context/` 最新 `Handover_*`）④ 關鍵檔位置（PRD/成果/規則）。
     - 自動 vs 人工：最新交接=自動抓最新 Handover；關鍵檔位置=半自動掃子夾；當前狀態/進行中任務=人工填語意。
     - 做法：寫 `wtf-config/scaffold_index.py`（延續 audit/organize 風格），掃 7 專案、自動填②④、語意區塊留 `<TODO>` 佔位、**只建不覆蓋**已存在 INDEX；先 dry-run 給使用者看再寫入。
   - lessons 逐專案完整彙整進 `wtf-config/LESSONS.md`（目前為指標式摘要）。
   - 科教館 `_context/` 檔名違規處理（Handoff 異體→Handover、prd.md/task.md/HANDOFF.md 改正名）；該專案在 WTF repo 外，須在其自身 repo 操作。
3. **承前次（工作區整合）**：T2 skills 漂移整治（tasklog-naming 已入 SSOT，其餘待盤點）、T4 進場→交接迴圈統一、T5 清孤兒 `AGENTS (1).md`（刪前確認）。
4. **承前次（程式repo移出Drive）**：
   - lesson「Drive 同步 .git 會損壞 repo」寫進 GLOBAL.md 檔案存放段。
   - 確認/修 lesson-add skill 路徑（claude-config→wtf-config）。
   - notebooklm-skill remote 決策（fork/換remote/維持本機）。
   - 跨機：其他桌機改 `~/.claude/CLAUDE.md` URL + clone `E:\Git_work` 各 repo。

## 3. 關鍵檔位置
- SSOT：`projects/WTF_Under_Construction/wtf-config/`（GLOBAL.md、LESSONS.md、AGENTS.md、machines.md、sync_config.py、audit_structure.py、organize_files.py、skills/）
- 程式 repo（非 Drive）：`E:\Git_work\`
- 稽核：`python wtf-config/audit_structure.py`；搬檔：`python wtf-config/organize_files.py [專案] [--apply]`

## 4. 注意事項
- 規則一律寫進 SSOT（wtf-config），不寫回根 CLAUDE.md（指標檔）。
- 覆蓋/刪除前必先確認。搬檔只搬不刪、同名不覆蓋。
- `register` 只在實體機跑，勿在 Cowork 沙盒。
