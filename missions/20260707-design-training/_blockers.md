# _blockers：design-training

## 2026-07-07 19:xx｜案例包來源連結無法「實開驗證」
**卡點**：本 mission 硬底線要求案例來源連結須「實際開啟並驗證過」（WebFetch）；本雲端 session 的組織出口政策全面擋 WebFetch（含 example.com、wikipedia.org、ruhrmuseum.de 均回 403，非個別網站擋爬蟲，主 session 與 subagent 皆已實測確認），故本棒無法產出符合硬底線的案例包。

**已找到候選**（WebSearch 摘要，未經 WebFetch 開啟）：魯爾博物館（Ruhr Museum）／關稅同盟煤礦選煤廠（Zeche Zollverein，UNESCO 世界遺產）——HG Merz 設計團隊在原選煤廠建築內插入獨立展示結構，約 80% 原始機具因文物保護原則原地保留，展線沿原「洗選煤生產動線」由上而下走。命中「工業機械題材」與錨-P1「主角是內容不是介面」判準，是扎實候選但未驗證。

**建議選項（待使用者裁決，擇一）**：
1. 等雲端 session 的 WebFetch 網路政策解除（可能是暫時性組織政策）後，下一棒重跑驗證，照原格式產出。
2. 使用者明確授權：本 mission 在雲端棒可改用「WebSearch 多來源交叉比對」作為驗證方式（取代 WebFetch 實開），檔案誠實標註驗證方式，不冒稱已開啟——此為放寬硬底線，需使用者裁決，弱模型不得自行改動綱領判準。
3. 使用者提供一個自己已驗證過的案例連結，本棒基於該連結做三問分析。
4. 案例包這項改列為「僅限本機」（Mac session 有無此網路限制待查），雲端棒固定跳過，只在本機執行棒才做。

**現狀**：跳過本項，backlog 案例包維持未勾；已寫入本檔待使用者處置，不空轉、不 parked（首棒零進展未達 2 棒門檻）。

## 2026-07-07 23:34 台北｜複查：同一卡點連續第 2 棒零進展 → 標 parked
複測 `/root/.ccr/__agentproxy/status` 診斷文件＋直接 WebFetch `https://www.ruhrmuseum.de/en/`：仍回 403，proxy README 明載「403/407＝組織出口政策不允許此 session 的目的地，不要重試或繞過——回報即可」，確認非暫時性、非個別網站問題，是本 session 全面政策封鎖。
本棒（第 2 棒）在同一卡點上無新進展可做（批評官值勤本週無使用者上傳照片、月審未到期，均不適用），依 `mission-loop.md` 停止閘「同一 mission 連續 2 棒零進展 → 標 parked＋通知」，本棒將 QUEUE 狀態改為 `parked`，待使用者從上方 4 個選項擇一裁決後再改回 `active`。

## 2026-07-21 台北｜使用者裁決：採選項4
案例包改列「僅限本機」，雲端棒固定跳過，只在本機（Mac）執行棒才做（backlog.md 已加註）。本週雲端可作項因此為 0（批評官值勤同樣本機限定、月審未到期），QUEUE 維持 `parked`——非待裁決卡點，是本週本來就無雲端可作項；使用者上傳照片或下次月審到期時可改回 `active`。
