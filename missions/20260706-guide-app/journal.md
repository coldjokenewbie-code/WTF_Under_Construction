# journal:guide-app

## 2026-07-07 09:50 台北|Fable 接手棒(使用者授權的零產出接手)|進展:yes
做了什麼:M1 全項——開場頁原型(真實資料:15站/14:00場次/三語)、Playwright 截圖與互動斷言、錨點對照(達正錨+超越點四項,見 out/before-after.md)。首版自審抓 3 版式缺陷,兩輪修正後複驗。
證據:out/開場頁_fable.html、out/shot_fable_開場頁.png、out/before-after.md。
狀態:M1 界達成 → QUEUE 改「待核准」。今晚小隊 M2 以本頁為錨。
2026-07-08 01:02｜執行棒（主 session 代跑）｜guide-app：現況稽核完成（8路由/24截圖/16改善項），關鍵發現=Tailwind CDN 依賴（沙盒實證全掛→截圖需 CDN 本地化後重驗）、dark 主題從未啟用、動態僅一處；已補判讀：優化第一增量=Tailwind 本地建置｜進展 yes｜證據：out/ui-audit_2026-07-08.md、out/audit-screenshots/
2026-07-08 21:43｜執行棒（主 session 代跑）｜guide-app：第一增量之一完成——Tailwind CDN→本地建置（tailwindcss@3.4.19+postcss+autoprefixer，config 逐項承接原 inline 設定，視覺零改動）；JetBrains Mono 改自架 woff2（31KB variable font）取代 Google Fonts，Zen Maru Gothic 確認全專案未引用故直接移除；tsc 過＋vite build 過；順手修 playwright.config.ts port 誤植(5173→3000)解除 npm test 卡死。分支因 per-repo designated-branch 限制改用 claude/admiring-franklin-a1okxy（非 ui-uplift，比照 o4-soundtrack 前例），已推 origin(commit 1b79407)。手動 chromium 走 4 頁截圖存證（沙盒無 webkit 故官方 mobile-onsite/web project 測不了，環境限制非本棒回歸）｜進展 yes｜證據：Assembly_Plant_Mobile_Guide commit 1b79407；missions/20260706-guide-app/out/tailwind-local-20260708/{after_landing,after_map,after_schedule,after_exhibit}.png
chain-capability: FAIL（今晚 19:34 o4-soundtrack 首棒漏測，本棒補測）。複測 CronCreate：durable 參數描述仍明寫「Has no effect — durable persistence is not available. All jobs are session-only」，session 結束即消失，無法做到棒內自建下一顆 trigger 讓新 session 接手。與 07-07 判定一致，連續 PASS 計數仍為 0。
2026-07-09 01:36｜執行棒（主 session 代跑，本棒輪替制取 guide-app，因 23:35 上一棒做的是 o4-soundtrack）｜guide-app：**第一增量之二完成**——fast-forward 合併上棒 commit 1b79407 到本 session 分支 claude/admiring-franklin-hxw35k（無新 code 改動，純取用已推上去的本地建置成果）；重拍全 17 個目標（8 路由＋9 個含互動態的變體：漢堡選單/tab 切換/地圖）存 `out/audit-screenshots-20260709/`，console-errors.json 顯示 0/17 pageerror（對照上輪 17/17 皆報 tailwind is not defined，證實增量一確實解決根因）；逐項重驗 audit 16 項改善清單真偽，寫入 ui-audit_2026-07-08.md「重驗結果」節：2 項（#4 opacity 底圖、#16 漢堡選單）確認是 CDN 失效造成的假象已隨增量一解決、1 項（#2 對比度）嚴重度下修為「可見但偏低對比」、12 項讀碼類發現（dark class/header文案/framer-motion單點/待補充排版/錯誤頁/縮圖風格/AR fallback/dev工具列/tab配色/孤兒檔案/字級）不受CDN影響全數確認為真維持原優先序、額外補充 #6（本地 tailwind.config.js plugins 仍為空陣列，animate-in 類別缺 plugin 非單純 CDN 遺留）；tsc 零錯誤＋vite build 過。分支推送受阻：本棒未在 Assembly_Plant_Mobile_Guide 產生新 commit（僅本地 fast-forward 供跑測試），故無需推送；若之後棒次要在該 repo 提交新 code，仍照前例走 designated branch claude/admiring-franklin-hxw35k｜進展 yes｜證據：missions/20260706-guide-app/out/ui-audit_2026-07-08.md（重驗結果節）、out/audit-screenshots-20260709/（17張+console-errors.json）、backlog.md 第二項打勾
chain-capability: 略（今晚已於 23:35 棒測過，FAIL，見上）
2026-07-09 19:34｜執行棒（主 session 代跑，本棒輪替制：上一棒 07-09 01:36 做的是 guide-app，依規則本應輪到 o4-soundtrack，但 o4-soundtrack 剩餘項全卡【討論閘】/待使用者本機執行，今晚可作項＝0——為避免空轉改續做 guide-app）｜guide-app：**第一增量之三完成**——修 audit 3 項讀碼類確定 bug：(1) AppShell.tsx:62 `getPageTitle` 對 `/exhibit/:id` 由固定回傳 `nav.scan`（誤植「掃描QRcode」）改為 `getExhibitById(id).name[language]`，`/exhibit/scan` 本身文案不變；(2) dev 工具列（375/680寬度切換/編輯內容/儲存/重置任務）加 `import.meta.env.DEV` 條件渲染，正式 build 不再顯示，連動调整 header/漢堡選單關閉鈕的 mt-9/top-9 offset 改依此旗標；新增 `vite-env.d.ts`（專案先前無 `import.meta.env` 型別來源，tsconfig `types` 僅列 `node`）；(3) 刪孤兒檔案 `components/InteractionPage.tsx`＋`pages/WorksheetPage.tsx`（grep 全專案確認零引用，真正掛路由的是同名的 `pages/InteractionPage.tsx`）。開棒前先發現：designated branch 本棒又換新（`claude/admiring-franklin-l8cmas`，非前棒 a1okxy/hxw35k），落後 origin/main 缺前棒 tailwind 本地建置 commit 1b79407——先 fast-forward merge a1okxy 補齊再開工。驗證：`npm install`＋`npx tsc --noEmit` 零錯誤、`npx vite build` 過；dev server 截圖確認 `/exhibit/G` header 顯示「組立進行中」（非「掃描QRcode」）、`/exhibit/scan` 不受影響仍顯示「掃描QRcode」；`vite preview`（production build）截圖確認 dev 工具列（「編輯內容」按鈕）計數＝0，header 定位正常無跑版｜進展 yes｜證據：Assembly_Plant_Mobile_Guide commit 59ba4ea（分支 claude/admiring-franklin-l8cmas，已推 origin）；missions/20260706-guide-app/out/increment3-20260709/{after_exhibit-G_header-fixed_dev.png,after_exhibit-scan_dev.png}；backlog.md 第三項打勾
chain-capability: FAIL（今晚首棒補測）。複測 CronCreate：`durable` 參數描述仍明寫「Has no effect — durable persistence is not available. All jobs are session-only」，與 07-07/07-08 判定一致，連續 PASS 計數仍為 0。
2026-07-09 21:34｜執行棒（主 session 代跑，本棒輪替制：上一棒 19:34 做的是 guide-app，依規則本應輪 o4-soundtrack，但其剩餘項全卡【討論閘】今晚可作項＝0，續做 guide-app 避免空轉）｜guide-app：**第一增量之四完成**——backlog「UI 整合：間距/字級/色彩 token 化」的第一小塊：字級 token 化（零視覺差異版）。開棒前先 fast-forward merge 上棒 `claude/admiring-franklin-l8cmas`（commit 59ba4ea）補齊，因本棒 harness 又換新 designated branch `claude/admiring-franklin-0yd8fy`。`tailwind.config.js` 新增 `fontSize.micro-{7,8,9,10,11,13,14}`，數值與現有 `text-[Npx]` 任意值一致；grep 全站（排除 `_context/archive` 歸檔舊碼）21 個檔案 81 處 `text-[Npx]` 用 sed 機械替換成對應具名 token，僅換 class 名不變數值。驗證：`npx tsc --noEmit` 零錯誤、`npx vite build` 過、逐一核對 dist CSS 中 `font-size:{7,8,9,10,11,13,14}px` 各出現 1 次（token 化前後 CSS 輸出完全相同）、dev server 4 頁截圖（exhibit-G/route-highlight/tasks/landing，存 `out/increment4-20260709/`）console 0 pageerror（僅 1 次 favicon 404 雜訊，與前棒一致非新增）。commit 94a71d5 已推 origin `claude/admiring-franklin-0yd8fy`。backlog.md 該項拆出子項並打勾第一小塊，留 3 個子項（間距系統化/色彩殘留token化、字級收斂 7→2-3 級、元件一致性如 #13 tab 色）給後續增量——收斂字級級距屬於會改變實際渲染大小的判斷，比照增量之一二三的謹慎原則，不在同一棒內夾帶視覺變動與零風險機械重構｜進展 yes｜證據：Assembly_Plant_Mobile_Guide commit 94a71d5；missions/20260706-guide-app/out/increment4-20260709/{after_exhibit-G,after_route-highlight,after_tasks,after_landing}.png；backlog.md 新增子項清單
chain-capability: 略（今晚已於 19:34 棒測過，FAIL，見上）
2026-07-09 23:34｜**定錨棒**（主 session 代跑，fresh-context opus 稽核，距 mission 首個執行棒 07-08 01:02 已滿 5 個執行棒，觸發定錨）｜guide-app：對照近 5 棒（audit→Tailwind本地化→全頁重驗→3項讀碼bug修正→字級token化）vs MISSION 方向句＋硬底線＋anchors 鐵律。**裁決：定錨 PASS**——5 棒皆落在「低品味風險先做」範圍，未越界碰高品味項/main/資料層；證據抽查 3 張截圖＋console-errors.json 與 journal 敘述吻合（如 increment3 截圖實測 header 確為「組立進行中」非「掃描QRcode」）。backlog 優先序維持不變，無需重排。稽核順手發現兩項記入 _blockers.md：(1) 提醒——連續多棒為零視覺差異機械重構，下一棒宜優先推進「間距系統化」或首個 framer-motion 轉場而非再挑安全項；(2) 標準演化提案——一旦動到會改外觀的項目（字級收斂/動態/色彩），硬底線「前後截圖」需升級為與 `展示頁面.md` 錨點並列比較句給四級等第，否則退回被禁的自評模式；(3) 補記——MISSION.md 引用的正錨路徑 `outputs/開場頁原型_20260707/` 實際不存在，現況是 `assembly-guide-trial/`，非偏航但路徑該對齊｜進展 yes（定錨屬品質把關，非新增量，無 backlog 勾項）｜證據：本則 journal 紀錄＋opus 稽核輸出（見本棒對話）
chain-capability: 略（今晚已於 19:34 棒測過，FAIL，見上）
2026-07-10 01:34｜執行棒（本棒輪替制：上一棒 07-09 23:34 是 guide-app 的定錨棒，o4-soundtrack 剩餘項全卡【討論閘】/需使用者本機執行今晚可作項仍為 0，續做 guide-app 避免空轉；且照定錨棒建議挑「動態」而非再挑安全項）｜guide-app：**第一增量之五完成**——修 audit #6：安裝 `tailwindcss-animate` 並註冊進 `tailwind.config.js` plugins（原空陣列），修復全站 5 個檔案已寫但從未生效的 `animate-in fade-in/zoom-in-*/slide-in-from-*` 類別（AppShell 漢堡選單/AudioPlayer 面板展開/TaskProgressBar×2/MapPage POI卡/EditableText 快捷選單）。開棒前先發現本 session designated branch（`claude/admiring-franklin-hds7tv`）落後 origin，缺前 4 棒累積的 commit（1b79407/59ba4ea/94a71d5）——fast-forward merge `origin/claude/admiring-franklin-0yd8fy` 補齊後才開工。驗證：`tsc --noEmit` 零錯誤、`vite build` 過、dist CSS 核對 `.animate-in`/`.fade-in`/`.slide-in-from-bottom` 規則從無到有、dev server 實測漢堡選單 overlay `getComputedStyle().animationName` 從 `none`→`enter`（0.3s）。CSS-only 動態，非 framer-motion 頁面轉場（audit #7 仍待做），標**待實機驗**（沙盒僅 headless chromium 無法目視流暢度）｜進展 yes｜證據：Assembly_Plant_Mobile_Guide commit ec34b7a（分支 claude/admiring-franklin-hds7tv，已推 origin）；missions/20260706-guide-app/out/increment5-20260710/{menu-mid-animation.png,menu-open-settled.png}；backlog.md 新增第一增量之五並打勾
chain-capability: 略（07-08/07-09 已連續測 FAIL，判定不變，暫停每棒重測避免重複記錄；累計 PASS 計數仍為 0）
2026-07-10 19:34｜執行棒（本棒輪替制：上一棒 07-10 01:34 是 guide-app，o4-soundtrack 剩餘項全卡【討論閘】/需使用者本機執行今晚可作項仍為 0，17:00 提醒棒快報已預告今晚 4 棒全數輪替回 guide-app）｜guide-app：開棒先 `git fetch`+fast-forward merge `origin/claude/admiring-franklin-hds7tv` 補齊本 session designated branch（`claude/admiring-franklin-72vb08`）落後的 4 個 commit。**間距系統化／色彩 token 化查核**：grep 全站任意值間距僅 1 處，試改具名 token 後 build CSS 規則消失（Tailwind 預設刻度跳過 13/15，60px 無對應 token）→ 判定原寫法合法非殘留，改回；色彩 grep 60+ 處字面色分類為圖片遮罩需真黑白／地圖圖例語意色兩類，均非殘留 bug，重配色屬已列 blockers 的高風險項不擅動→backlog 該項勾選（無需改碼）。**第一增量之六：頁面轉場**——修 audit #7 前半：新增 `components/PageTransition.tsx`，`App.tsx` 用 `AnimatePresence mode="wait"` + `Routes location/key` 使切換時舊頁維持掛載播放退場動畫（RoutesProps 無 `key` 型別因專案未裝 @types/react，加註解說明的 `@ts-expect-error`）。tsc 零錯誤＋vite build 過；chromium 實測點擊底部導覽（client-side navigate 非整頁 reload）3 段切換，mid-transition 截圖確認退場疊化生效，console 404 經 git stash 比對確認 baseline 既有非本次引入。字級收斂／元件一致性兩項因待裁決 anchor 升級提案未定，本棒不碰，維持卡點狀態｜進展 yes｜證據：Assembly_Plant_Mobile_Guide commit 13bd398（分支 claude/admiring-franklin-72vb08，已推 origin）；missions/20260706-guide-app/out/increment6-route-transition-20260710/{rt-01~06}.png；backlog.md 更新（間距色彩項打勾＋新增第一增量之六並打勾）
chain-capability: 略（07-08/07-09/07-10 01:34 已連續測 FAIL，判定不變，暫停每棒重測；累計 PASS 計數仍為 0）
2026-07-10 21:34｜執行棒（本棒輪替制：上一棒 19:34 是 guide-app；本棒開棒先查 QUEUE 現況＝machine-report 已於 07-07 23:34 棒改「待核准」且 backlog 全數完成，o4-soundtrack 全部剩餘項仍卡【討論閘】/本機執行，design-training 本週唯一可作項「案例包」仍卡 WebFetch，本棒實測 `curl https://www.ruhrmuseum.de/en/` 仍回 403，確認組織出口政策未解除，維持 parked——三案皆無可作項，續做 guide-app）｜guide-app：**第一增量之七：清單進場 stagger（修 audit #7 後半，framer-motion 動態最後一塊）**。開棒先 fast-forward merge `origin/claude/admiring-franklin-72vb08`（commit 13bd398）補齊本 session designated branch `claude/admiring-franklin-cwxz2l` 落後的 5 個 commit。新增 `components/StaggerList.tsx`（`motion.div` container，`staggerChildren:0.06`；`staggerItemVariants`：opacity 0→1＋y 8→0，250ms easeOut），套用到三處 audit 明點的清單：`NearbyExhibits.tsx` 附近展品卡片（`motion.button`）、`SchedulePage.tsx` 時間軸項目（`motion.div`，確認 `ref` callback 對 motion.div 轉發的 DOM 節點仍相容）、`ExhibitPage.tsx` `ScanView` 展品搜尋結果（`motion.button`）。**驗證中意外發現既有 bug**：`ScanView` 的 `allExhibits` 用 `getExhibitById('E01'~'E20')` 產生候選清單，但真實展品 id 是 `G/0/1A/1B/1C/2A/2B/2C/2D/3/4A/4B/4C/5` 這種格式，20 次呼叫全部回傳 `undefined` 被 filter 清空——展品搜尋功能無論輸入什麼關鍵字，`filteredExhibits` 恆為空陣列，UI 固定顯示「找不到相關展品」，等於搜尋框從未真正運作過。此 bug 與本次 stagger 改動無關（純既有缺陷），但直接卡住本棒對搜尋清單 stagger 的實機驗證，故一併用一行修正：改直接 `import { EXHIBITS }` 取代錯誤的 id 迴圈生成。驗證：`npx tsc --noEmit` 零錯誤、`npm run build` 過；chromium 三處實機測試（`/schedule` 進場截圖捕到第二項仍在淡入位移中；`/exhibit/scan` 搜尋「機」捕到第三項仍在位移、settled 後正確顯示 4 筆真實結果——證實 bug 已修好；`/exhibit/1A` 附近展品卡片 settled 畫面確認渲染正確，因單卡於 viewport 內動畫過快未捕到 mid-frame）；console 僅既有基線 404 無新增錯誤。verify 用臨時 Playwright 腳本未納入 commit（scripts/ 為一次性驗證用，非專案既有目錄）。commit e25f363 已推 origin `claude/admiring-franklin-cwxz2l`。**未做**：按壓回饋微互動（tap/press feedback），framer-motion 動態最後一項，留下一增量；做完後 audit #7 三個子項（頁面轉場/清單stagger/按壓回饋）將全數完成｜進展 yes｜證據：Assembly_Plant_Mobile_Guide commit e25f363；missions/20260706-guide-app/out/increment7-list-stagger-20260710/{schedule-mid,schedule-settled,exhibit-search-mid,exhibit-search-settled,nearby-mid,nearby-settled}.png；backlog.md 新增第一增量之七並打勾
chain-capability: 略（07-08~07-10 已連續測 FAIL，判定不變，暫停每棒重測；累計 PASS 計數仍為 0）
2026-07-10 23:35｜執行棒（本棒輪替制：上一棒 21:34 是 guide-app；本棒開棒查 o4-soundtrack journal，最後一筆為 07-08 23:35，剩餘項全卡【討論閘】方向二拍板/本機執行，可作項仍為 0，續做 guide-app）｜guide-app：**第一增量之八：按壓回饋微互動（修 audit #7 最後一塊，framer-motion 動態子項全數收官）**。開棒先 fast-forward merge `origin/claude/admiring-franklin-cwxz2l`（commit e25f363）補齊本 session designated branch `claude/admiring-franklin-9jciso` 落後的 commit。全站 grep 所有 `<button>`／可點擊互動 div，發現多數主要 CTA（AudiencePage 推薦卡、NearbyExhibits、MapPage 縮放列、NarrationPlayer 播放鍵）已具備 `active:scale-90/95/[0.98]` 慣例（既有設計系統，非本棒新創），本增量只補齊缺漏的觸控目標，維持同一慣例（CSS `:active` 偽類，非改用 framer-motion whileTap，避免與既有模式混雜）。涵蓋 13 檔：`AppShell.tsx`（底部導覽 4 tab／漢堡選單開關／語言切換／選單項卡片）、`ExhibitHeader.tsx`（返回鍵／語言切換）、`ShowTimeWidget.tsx`、`AudioPlayer.tsx`（展開/變速/快轉）、`TaskBanner.tsx`（開始任務/提交答案）、`ExhibitPage.tsx`（搜尋結果列/展品分頁列）、`RoutePage.tsx`、`SchedulePage.tsx`（收藏星號）、`InteractionPage.tsx`（重新拼裝鍵）、`ARPage.tsx`（縮放/掃描/特效鍵）、`MapPage.tsx`（POI 標記按下放大/查看展品鍵）、`LandingPage.tsx`（展開/時刻表徽章/搜尋結果/CTA）、`TasksPage.tsx`（送出email鍵）。**刻意跳過**：dev-only 編輯工具（`isEditMode`/`SHOW_DEV_TOOLBAR` 閘控，訪客不可見，比照增量三前例不對開發工具加視覺）、既有拖曳互動（`InteractionPage` 拼圖，加 active 會與 drag 手勢衝突）、純圖示型 X 關閉鍵（沿用全站既有慣例，close/clear 圖示鍵本就不做按壓回饋）。**過程中一次技術判讀修正**：一度用 `active:bg-zinc-800/70` 做列表列按壓回饋，build 後 dist CSS 逐條核對發現該 class 完全沒編譯出來——查明原因是 `tailwind.config.js` 的 zinc 色票是 CSS 變數字串（`var(--color-zinc-800)`）非 Tailwind 要求的 `rgb(<alpha-value>)` 格式，無法套用透明度修飾符，Tailwind 靜默丟棄該 utility；改回 `active:scale-[0.98]` 解決，此為本棒抓到的既有色票設計限制記錄供未來參考。驗證：`npx tsc --noEmit` 零錯誤、`npm run build` 過；dist CSS 逐條 grep 核對 4 種 `active:scale-{90,95,110,[0.98]}` 皆確實編譯出對應 `:active` 規則；chromium 實機測試（mouse down 保壓 120ms 讀 `getComputedStyle().transform`）4 處觸控目標——底部導覽 tab（scale≈0.94）、展品頁分頁列（scale≈0.95）、首頁「下一站」CTA（scale≈0.95）、地圖 POI 標記（scale=1.1，設計為放大而非縮小）——全數確認按下時產生視覺回饋；一次 console 404 經 `page.on('response')` 覆查為間歇性雜訊非本棒引入。臨時驗證用 Playwright 腳本未納入 commit（沙盒一次性用途）。commit a4c5fc7 已推 origin `claude/admiring-franklin-9jciso`。**M2 界判定**：backlog 增量一～八為目前所有不需使用者裁決即可做的低品味風險項，已全數完成；僅剩「字級收斂」「元件一致性修整」兩項卡在 `_blockers.md` 2026-07-09 定錨棒提出的「anchor 升級提案」待裁決，非技術債務而是決策閘，繼續做會落入被禁的自評模式。判定 guide-app 已達 M2 界，backlog.md 該行標記完成，QUEUE 狀態本棒改「待核准」，等待使用者驗貨＋一次裁決 4 項 blockers（2 項 M1 高品味主題/縮圖路線、1 項 anchor 升級提案、1 項路徑訂正）。o4-soundtrack 剩餘項仍全卡拍板/本機執行，今晚循環棒至此兩個 active mission 皆無可作項，預期下一棒秒退或轉往其他候選｜進展 yes｜證據：Assembly_Plant_Mobile_Guide commit a4c5fc7；missions/20260706-guide-app/out/increment8-press-feedback-20260710/{landing-before,nav-tab-pressed}.png；backlog.md 第 13/16/18 行更新；QUEUE.md guide-app 行狀態改「待核准」
chain-capability: 略（07-08~07-10 21:34 已連續測 FAIL，判定不變，暫停每棒重測；累計 PASS 計數仍為 0）

## 2026-07-21 台北｜使用者裁決｜進展：no
使用者針對 M1 兩項高品味 blockers（全站主題定調、氛圍底圖/展品縮圖/hero風格統一）裁決：**工業風、暗色為主，禁用可愛風／卡通風（icon 使用時尤其注意）**。已寫入 MISSION.md 視覺風格定調段＋_blockers.md 兩條標記解決＋backlog.md 對應項附註方向。剩餘唯一卡點：anchor 升級提案（字級收斂/元件一致性修整的驗收方法論）仍待使用者裁決，已於對話中提示建議採納，回覆待補。順手訂正 MISSION.md 正錨路徑引用（`outputs/開場頁原型_20260707/`→`assembly-guide-trial/`，_blockers.md 2026-07-09 低優先補記項）。
證據：MISSION.md（新增風格定調段＋路徑訂正）、_blockers.md（2項標記解決）、backlog.md（字級收斂/元件一致性項附註）
